fn main() {
    // If-else-if ladder
    // If NUM is 1, print ► You won!
    // Else if NUM is 2, print Try again.
    // Else print You Lost!

    let num = 0;

    if num == 1 { 
    // It must be double equal (==).
        println!("You won!");
    } else if num == 2 {
        println!("Try again.")
    } else {
        println!("You lost!");
    }
}
